WHat is data?
collection of raw bytes 
like images in collection of bytes

A data when it is processed gives us some meaning and from that we can extract information...

processed data is called information

a dbms is collection of database to store the data and set of programs so that the data stored in database can be adding, updating, filtered and deleted and ..

now dbms vs file system is explained in the video with examples of bank system do look into it....

